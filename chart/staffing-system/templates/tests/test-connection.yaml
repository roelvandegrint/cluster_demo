apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "staffing-system.fullname" . }}-test-connection"
  labels:
    {{- include "staffing-system.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "staffing-system.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
